[package]
name = "wasm-engine"
version = "0.1.0"
edition = "2021"

[lib] 
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
image_crate = {package = "image", version = "0.24", features = ["png", "jpeg"] }
printpdf = { version = "0.6", default-features = false, features = ["image"]}